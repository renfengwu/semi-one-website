# 00 · 项目上下文 〔本项目活记忆 · AI 维护〕

> **作用**:这是项目的“身份档案”。AI 接管项目时先读这里,了解项目目标、技术栈、目录、部署取值。
> **更新时机**:架构、技术栈、目录结构、端口、部署目录、重要约束变化时更新。

---

## 1. 项目是什么

- **项目名称**:`芯电元官网升级 / Semi-One Corporate Website Upgrade`
- **一句话目标**:把旧官网 `http://www.semi-one.com/` 升级为有科技感、权威感、可检索、可持续发布的功率半导体企业官网,并完整跑通 GitHub Flow + CI + CD。首个自主闭环 CD 采用 GitHub Pages;SSH/Docker 服务器部署在取得服务器信息后追加。
- **使用者/受益者**:
  - 电子工程师、采购、FAE、渠道伙伴:快速理解芯电元能力并筛选产品。
  - 潜在客户与合作伙伴:确认研发、质量、供应链和全球服务能力。
  - 公司市场/销售/技术团队:沉淀产品、应用、证书、技术文章和联系线索。
  - 开发/维护者:通过标准化工程结构、测试、CI/CD 安全迭代。
- **核心功能**:
  - 科技感首页:展示功率半导体定位、研发路线、质量背书、全球布局和关键入口。
  - 产品中心:按 MOSFET、功率 IC、TVS、IGBT、模拟开关等分类展示,支持参数搜索、筛选、排序与数据手册入口。
  - 应用方案:覆盖 3C 数码电池、BMS/储能、电动工具、无线充、电机驱动、电子烟、PD 等场景。
  - 技术与创新:展示 Trench MOS、SGT MOS、SJ MOS、IGBT、SiC/GaN 路线、实验室设备和可靠性能力。
  - 质量与资质:展示 ISO 9001、ISO 14001、ISO 45001、ISO 37301、知识产权合规、服务认证、专利/软著等材料,上线前需人工复核证书编号、有效期和公开范围。
  - 关于与联系:保留中文/英文对外表达,记录越南语入口为后续确认项;展示深圳总部及美国、越南、香港等联系信息。
  - 工程化与发布:通过 feature 分支、PR、CI、CD、健康检查完成自动部署闭环。
- **输入/数据**:
  - 旧官网公开内容:`http://www.semi-one.com/`。已确认旧站导航含首页、产品、应用、技术支持、关于我们,旧站有中文/英文/越南语入口。
  - 本地资料:`data/芯电元公司简介2025中文.pptx`、`data/芯电元公司简介PPT 英文pptx.pptx`、`data/芯电元体系证书/` 下 PDF/XLS、活动图片/视频。
  - 资料要点:公司 2011 年成立,专注功率半导体研发、制造、销售和技术服务;国家高新技术企业、专精特新企业;研发团队 40+;2024 年销售额近 4.5 亿元人民币,2025 年预计突破 5 亿元;研发/销售网络覆盖深圳、无锡、佛山、上海、香港、台湾、美国、越南;PPT 记录发明专利 52 项、实用新型 16 项、布图设计 31 项。
  - 数据治理:原始大文件、扫描证书、活动照片/视频默认只作参考源,不直接进入生产构建;需筛选、压缩、脱敏并经人工确认后复制到 `public/assets/` 或结构化内容目录。

## 2. 技术栈

| 层            | 选型                                                      | 理由                                                                                        |
| ------------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 语言/运行时   | TypeScript + Node 24                                      | 本机已验证 Node `v24.15.0`;TypeScript 适合长期维护产品数据、组件和测试。                    |
| Web/API 框架  | Vite + React                                              | 企业官网首版以静态站为主,构建快、部署简单、适合组件化和交互式产品筛选。                     |
| 样式/UI       | Tailwind CSS + 少量 CSS Modules/全局 tokens               | 便于快速建立科技感视觉系统,同时保持一致间距、断点、色彩和组件状态。                         |
| 测试          | Vitest + Testing Library + Playwright                     | 单元/组件测试覆盖产品筛选、内容渲染和交互;Playwright 覆盖首页、产品、联系、移动端关键路径。 |
| 格式/静态检查 | ESLint + Prettier + `tsc --noEmit`                        | 前端 TypeScript 项目的常规质量门禁,可在本地和 CI 一致执行。                                 |
| 打包/运行     | Vite build + GitHub Pages;Docker + Nginx 作为服务器部署包 | 构建产物小;Pages 可在没有外部服务器信息时自主跑通 CD;Docker/Nginx 保留后续服务器部署能力。  |
| CI/CD         | GitHub Actions + GitHub Pages                             | 通用、可视化、适合教学与团队协作;PR 跑 CI,main 合并后自动发布 Pages。                       |

## 3. 目录地图

> 当前项目尚未创建源码,根目录目前只有 `standards/` 与 `data/`。以下是确认后实施的目标目录地图;新增目录前先更新本节。

```text
<project>/
├── standards/                 # AI 项目记忆与通用规范
├── data/                      # 参考资料源;默认不直接进入生产构建
├── public/
│   ├── assets/                # 经确认、压缩后的公开图片/证书/品牌素材
│   └── health                 # CD 健康检查静态文件
├── src/
│   ├── app/                   # 路由、布局、语言切换、全站状态
│   ├── components/            # 可复用 UI 组件
│   ├── content/               # 页面文案、SEO、多语言内容
│   ├── data/                  # 产品、应用、证书、时间线等结构化数据
│   ├── pages/                 # 首页、产品、应用、技术、质量、关于、联系
│   ├── styles/                # 设计 tokens、全局样式
│   └── test/                  # 测试工具和 fixture
├── tests/
│   ├── unit/                  # Vitest 单元/组件测试
│   └── e2e/                   # Playwright 关键路径测试
├── scripts/                   # 数据抽取、图片压缩、部署辅助脚本
├── .github/workflows/
│   ├── ci.yml
│   └── cd.yml
├── package.json
├── vite.config.ts
├── tsconfig.json
├── Dockerfile
├── nginx.conf
└── README.md
```

## 4. 质量门槛

| 类型          | 本项目标准                                                                                                    |
| ------------- | ------------------------------------------------------------------------------------------------------------- |
| 格式检查      | `npm run format:check`                                                                                        |
| 静态检查      | `npm run lint` + `npm run typecheck`                                                                          |
| 单元测试      | `npm run test:run`                                                                                            |
| 覆盖率        | `npm run test:coverage`,核心逻辑/组件覆盖率 `>=80%`                                                           |
| E2E/可访问性  | `npm run test:e2e`,覆盖桌面与移动端：首页、产品筛选、应用入口、联系页、语言切换、无明显重叠/空白              |
| 构建          | `npm run build`;CI 额外执行 `docker build`                                                                    |
| 业务/发布门禁 | 产品筛选可用;关键页面 SEO metadata 完整;Pages CD 成功并输出站点 URL;服务器部署阶段再验证 `/health` 与最终端口 |

## 5. 不变约束

- 密钥、密码、私钥、Token **绝不写进代码或文档**,只进 GitHub Secrets / 环境变量。
- `main` 分支受保护,日常开发必须走 feature 分支 + PR;AI 不自行合并 PR。
- CI 红灯不合并;flaky test 必须根治。
- 不上线空白占位、模板废话、未授权客户 Logo、未复核证书编号/日期或无法溯源的夸大表述。
- 活动照片、视频、ARW 原片和 PPT/PDF/XLS 原始资料默认不进生产构建;是否纳入 Git、Git LFS 或外部对象存储需单独确认。
- 官网文案优先使用本地 PPT、证书、XLS 和旧站公开内容;涉及客户、证书、财务规模、专利数量等权威信息,上线前需人工确认公开口径。

## 6. 部署/CI 占位符取值

| 占位符          | 本项目取值                                          | 说明                                                   |
| --------------- | --------------------------------------------------- | ------------------------------------------------------ |
| `<APP>`         | `semi-one-website`                                  | 应用名/镜像名/容器名                                   |
| `<DEPLOY_DIR>`  | `GitHub Pages`;服务器部署为 `/opt/semi-one-website` | 首个 CD 目标为 Pages;服务器目录待服务器确认            |
| `<PORT>`        | `8080`                                              | 主机优先端口;2026-07-03 本机检查未占用                 |
| `<PORT_MAX>`    | `8089`                                              | 主机端口回退区间上限;CD 不盲删他人容器                 |
| `<PYVER>`       | `Node 24`                                           | 模板字段沿用,本项目实际是 Node runtime                 |
| `<HEALTHCHECK>` | `/health`                                           | 服务器部署阶段使用;Pages 阶段以 Pages URL 访问成功为准 |
| `<SSH_USER>`    | `<服务器部署待提供>`                                | Pages 阶段不需要;服务器部署阶段再配置                  |
| `<SSH_HOST>`    | `<服务器部署待提供>`                                | Pages 阶段不需要;服务器部署阶段再配置                  |
